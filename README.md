# funny web extension that uses palm ai from makersuite to make funny subtitle to the youtube video :D

to make at src folder file with name config.js and make virable ``export const API_KEY = "[API_KEY_HERE]"``
